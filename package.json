{
  "scripts": {
    "dev": "vite --port 5188",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "simulate": "deno run --unstable-sloppy-imports --allow-read --allow-write scripts/simulation-runner.ts",
    "deploy": "npm run build && bash deploy.sh",
    "deploy:beta": "npm run build && bash deploy-beta.sh"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
